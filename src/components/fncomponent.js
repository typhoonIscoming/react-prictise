import React from 'react';
// react 16.8开始支持hook
// import './functionalCom.scss';

function functionalComponent() {
    return (
        <div className="functional-component-container">
            函数式组件
            {/* <p onClick={setCount(count + 1)}>点击增加, count = {count}</p> */}
        </div>
    )
}

export default functionalComponent
